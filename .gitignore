node_modules/
dist/
public/models/
# Runtime-ul ONNX (~26 MB de wasm) — vine din node_modules la build, ca si modelele.
public/ort/
# lista de localitati pentru ecranul Locatii — adusa de CI la build (GeoNames),
# ca si modelele AI de mai sus; vezi pasul "Bundle place names" din workflow-uri
public/places/
release/
*.local

# cheia de semnare Android (Play Store) — NICIODATA in git, vezi
# android/app/keystore.properties.example pentru cum se genereaza local
android/app/keystore.properties
*.jks
*.keystore

# libraw-wasm rebuild vendorizat (vendor/libraw-wasm-patched/README.md) — dist/ de aici
# e payload-ul compilat al dependentei, nu un artefact de build al PROIECTULUI nostru
# (cel ignorat de regula "dist/" de mai sus), deci trebuie commis in git.
!vendor/libraw-wasm-patched/dist/

# arhiva de livrare, se regenereaza cu scripts/store-screenshots.py
store/lumin-culler-play-store.zip
